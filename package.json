{
  "name": "navhub",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "个人网址导航 - 纯前端静态站点（GitHub Pages / Cloudflare Workers 双部署）",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npx serve .",
    "deploy": "npm run build && wrangler deploy",
    "deploy:pages": "npm run build && wrangler pages deploy dist --project-name=navhub"
  },
  "cloudflare": {
    "label": "导航网站",
    "products": [
      "Workers"
    ],
    "bindings": {}
  },
  "devDependencies": {
    "wrangler": "^4.119.0"
  }
}
